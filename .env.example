# Cypress Cloud record key — get this from
# https://cloud.cypress.io/projects/7jh9s1/settings
# Copy this file to `.env` and fill in the real value.
# `.env` is gitignored — never commit your actual key.
CYPRESS_RECORD_KEY=
